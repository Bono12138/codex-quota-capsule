# v0.3.1-beta.1 Release Evidence

Status: final candidate
Updated: 2026-07-14

## Automated verification

- The final candidate must pass TypeScript/Vitest, Swift Testing, shared polling-equivalence fixtures, builds, lint, repository policy, Weekly Only copy audit, packaging, and `git diff --check` from a clean tracked tree.
- Reset-credit parsing, history migration, privacy schema, lifecycle classification, localization, presentation ordering, and destructive cleanup are covered by automated tests. Destructive cleanup uses an isolated temporary database rather than the user's live history.
- The repository audit permits reset-credit lifecycle vocabulary only on explicit reset-credit lines; retired Weekly Only state labels remain rejected.

## Installed app verification

- The candidate was installed at `/Applications/Quota Capsule Beta.app` and read the live Codex weekly allowance successfully.
- The collapsed view contains no reset-credit detail. The expanded view places the reset-credit bank last, shows the authoritative count, and sorts four available expiries through the local minute.
- The language menu remained open with all three choices after an uninterrupted 11-second observation while background polling continued.
- Exact timestamps and the recovered expired-credit observation are preserved only in an ignored local evidence directory and the local app database. No raw provider identifier or description is tracked.
- Exact merged-main version, signature, source fingerprint, artifact checksum, process path, and final installed interaction are mandatory again before upload.

## Code review

- Review covered polling invariance, robust-estimator boundary behavior, cross-zero forecast wording, status-menu lifetime, malformed and capped reset-credit payloads, SQLite migration and bound writes, privacy columns, conservative lifecycle transitions, localization, ordering, and stale-snapshot preservation.
- The implementation intentionally keeps reset credits outside the forecast risk state. It does not claim redemption, recommend redemption timing, or infer a fixed issuance cadence from the current small sample.
- The first passes found and fixed polling-dependent activity evidence, misleading clamped reset ranges, mutable menu presentation, and an over-broad copy audit rule. Each regression now has an automated test.

## Pull request

- Public pull request, required `node` and `macos-swift` checks, and merged-main commit are pending publication.
- Only commits authored and committed by `Bono12138 <Bono12138@users.noreply.github.com>` are eligible for the release branch.

## Release status

Final candidate. Publication remains blocked until the clean candidate passes every local gate, the public pull request and required CI checks pass, the exact merged `main` commit is packaged and installed, and the GitHub prerelease artifact is verified.

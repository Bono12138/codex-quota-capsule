# v0.3.4-beta.1 Release Evidence

Status: candidate verified locally; publication pending PR review and merged-main rebuild
Updated: 2026-07-18

## Defect and root cause

- Public issue [#26](https://github.com/Bono12138/codex-quota-capsule/issues/26) records a P0 mismatch: Codex and a direct probe reported 2% used / 98% remaining while Quota Capsule remained at its previously accepted 0% reading.
- Successful 1% and 2% readings were already present in SQLite. The failure was therefore in the quality transition, not the Codex source adapter or percentage formatting.
- The 0.3.3 unused-window exception assumed uninterrupted polling and required provisional reset movement to match the observation gap. After an application gap, the reset shifted by about 4.5 hours: too different for that exception, but below the six-hour new-cycle threshold. The candidate was never added to a confirmation buffer and could calibrate forever.

## Repair

- A valid non-backward reset may replace zero-only provisional history even after sleep, restart, or a long polling gap. Positive use ends this exception immediately.
- Every persistent reset-time correction now has a confirmation exit path. Without a new-cycle signal, three consistent readings over two minutes rebase the same cycle instead of creating a false new cycle or remaining in calibration.
- Swift, TypeScript, and the shared cross-runtime fixture cover the application-gap sequence and the positive-use reset-time correction.
- Confirmation copy now says that the latest read succeeded while previous confirmed data is temporarily displayed.

## Automated verification

- The new Swift application-gap regression first failed with `calibrating`, an accepted `[0]` history, and the obsolete reset; it passes after the repair.
- The new persistent-correction Swift regression first failed with `calibrating` and the old reset; it passes after the repair.
- Both equivalent TypeScript regressions first failed with `calibrating`; both pass after parity implementation.
- Candidate verification passes 110 Node/Vitest tests, 94 Swift tests, TypeScript and Swift builds, lint, `QuotaCapsuleCoreSpec`, the 49-file Weekly Only audit, repository policy, and `git diff --check`.

## Installed app verification

- A locally built 0.3.4 candidate was installed at `/Applications/Quota Capsule Beta.app`; its ad-hoc signature passed strict verification and exactly one `QuotaCapsuleBeta` process ran from that bundle.
- On launch, the candidate replayed the existing error history and adopted the live 3% used / 97% remaining reading with the July 25 12:48 local reset instead of remaining at 0%.
- A later automatic refresh kept the interface, direct read-only probe, and SQLite at 3% / 97%, advanced the visible data-read time, and retained the same reset.
- The expanded panel also displayed all three available reset-credit expiry times through the local minute after a complete source response.
- No Codex authentication, binary, configuration, or login state was changed.

## Code review

- The repair removes the unreachable middle range in the reset state machine: a stable reset correction no longer needs to be either five-minute jitter or a six-hour new-cycle jump.
- The zero-only rebase cannot rewrite positive-use evidence because it is disabled as soon as any accepted active-cycle reading is above 0%.
- A confirmed correction without a new-cycle signal keeps the same cycle identifier and rewrites its canonical reset; a confirmed six-hour shift or two-point usage drop retains the existing new-cycle behavior.
- Independent reviewer feedback and required CI remain pending on the public pull request.

## Pull request

- Public P0 issue [#26](https://github.com/Bono12138/codex-quota-capsule/issues/26) is the defect source of truth and links the remaining store-level hardening issue #18.
- The implementation branch is `codex/clarify-quota-confirmation`. Pull-request URL, review result, CI checks, squash-merge commit, and branch deletion will be recorded after publication.

## Publication verification

- The installed candidate intentionally carries a non-empty source-patch fingerprint and is not a release artifact.
- Publication remains blocked until the public pull request passes required CI, merges to `main`, and the exact merged commit is rebuilt, signature-checked, installed, compared with a live probe and SQLite, packaged, checksummed, tagged, and uploaded as a prerelease.
- A final evidence-only update must record the merged commit, PR, CI runs, tag, release URL, ZIP checksum, downloaded-asset checksum, installed empty-patch fingerprint, and branch cleanup.

## Release status

`v0.3.4-beta.1` is not released yet. Local candidate verification is complete; public review, required CI, exact merged-main packaging, tagging, upload, downloaded-asset verification, and final evidence are still open gates.

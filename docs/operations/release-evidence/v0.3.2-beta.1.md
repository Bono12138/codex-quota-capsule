# v0.3.2-beta.1 Release Evidence

Status: final candidate
Updated: 2026-07-14

## Automated verification

- The source-architecture regression test failed while `PanelQuickActionsView` contained two nested SwiftUI `Menu` layers and passes only when the floating panel has one menu layer with three top-level language actions.
- The final branch candidate passed 106 Node/Vitest tests, 89 Swift tests, the cross-language core specification, TypeScript and Swift builds, lint, repository policy, the 49-file Weekly Only copy audit, packaging, archive integrity, and `git diff --check`.
- The branch-candidate app reported version `0.3.2`, commit `deea4a1c732b`, and an empty source-patch fingerprint. Its ad-hoc signature verified with `codesign --verify --deep --strict`.

## Installed app verification

- The supplied 9.408-second recording was extracted at approximately 56.5 frames per second. Frame inspection showed the language submenu disappearing as the pointer crossed between a right-edge parent menu and its left-opening child.
- An unchanged `v0.3.1` installation was sampled 80 times over 18.671 seconds, spanning an automatic data read. With the pointer stationary, neither menu layer changed; this ruled out the refresh loop as the trigger for the recorded path.
- The installed `v0.3.2` candidate showed exactly one More Actions menu containing the three language choices directly, with no child menu.
- The candidate was sampled continuously 180 times over 42.597 seconds while the menu remained open. The single menu and all language actions stayed present throughout a real automatic read; after the check, the panel showed a new successful update at `15:05:25` and the next-read countdown had restarted.

## Code review

- Root cause: `v0.3.1` stabilized the AppKit status-bar menu, while the recording used a separate nested SwiftUI menu inside the floating panel. The previous endpoint-only check was applied to the wrong surface and could not detect intermediate disappearance.
- The fix is intentionally limited to flattening the panel language actions. Forecasting, refresh cadence, reset-credit storage, and the AppKit status menu are unchanged.
- Competitor and OpenAI-window research was refreshed from current primary sources. The public positioning now treats Weekly Only as the current reliable product surface, not a promise that an upstream short window can never return.

## Pull request

- Public [pull request #20](https://github.com/Bono12138/codex-quota-capsule/pull/20) passed the required `node` and `macos-swift` checks for the reviewed branch.
- The merged-main commit, tag CI, and release artifact are still pending publication.
- The release must retain `Bono12138` as the only GitHub contributor.

## Release status

Final candidate. Publication remains blocked until installed-app interaction, clean local gates, public CI, exact merged-main packaging, signature verification, tag CI, and remote artifact checksum verification all pass.

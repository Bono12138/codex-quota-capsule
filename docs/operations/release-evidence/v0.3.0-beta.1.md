# v0.3.0-beta.1 Release Evidence

Status: release candidate under review
Updated: 2026-07-13

## Automated verification

- TypeScript/Vitest, Swift Testing, shared-fixture parity, builds, lint, repository policy, and Weekly Only copy audit are required on the final candidate.
- The updated candidate passes 91 TypeScript tests and 55 Swift tests, including new reset-candidate, first-zero, stale-presentation, activity-segmentation, release-audit, copy-semantics, and archive-integrity regressions.
- The repository audit now checks release metadata, internal Markdown links, required release evidence, credentials, personal paths, generated output, obsolete Dev identities, fixed six-hour gates, and hidden reserves.

## Installed app verification

- Candidate `3566421` was packaged as version `0.3.0`, signature-verified, and installed at `/Applications/Quota Capsule Beta.app`.
- The installed app completed live read, expand/collapse, manual refresh, diagnostics, menu, reset-time, and next-read checks against the read-only local history database.
- A new exact-commit package and live check are required after the review fixes and again from merged `main` before the release asset is uploaded.

## Code review

- Independent pre-merge review blocked the first candidate on reset-candidate isolation, first-zero wording, stale forecast leakage, activity segmentation, release-audit coverage, and final archive checksums.
- Every accepted finding was reproduced with a failing regression test before implementation. Re-review must still report no Critical or Important findings before merge.

## Pull request

- PR: [#17 — Ship adaptive Weekly Only forecast and single-repo workflow](https://github.com/Bono12138/codex-quota-capsule/pull/17)
- Required checks: `node` and `macos-swift`.
- Merge is blocked until the updated candidate passes CI and re-review.

## Release status

Blocked pending exact-commit installed-app verification, re-review, updated PR CI, merged-main artifact checksum, tag, and GitHub prerelease publication.

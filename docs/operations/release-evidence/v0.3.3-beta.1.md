# v0.3.3-beta.1 Release Evidence

Status: released and independently verified
Updated: 2026-07-15

## Defect and root cause

- Public issue #23 records a P0 case where Codex's live weekly window advanced from 0% to positive usage while Quota Capsule kept displaying the first 0% reading and an obsolete reset time.
- Before first use, the upstream reset timestamp advanced with observation time for about 5.5 hours. The quality engine treated every shift beyond its five-minute jitter cluster as an unconfirmed reset candidate, but the sequence never reached the six-hour new-cycle threshold.
- Raw successful readings continued to enter SQLite. `QuotaRefreshReducer` correctly refused the unconfirmed candidate, which unintentionally froze the visible accepted snapshot instead of adopting the anchored first-use window.

## Automated verification

- The Swift regression first failed with `calibrating`, `shouldAdoptLiveSnapshot = false`, and a 0% forecast instead of the live positive reading.
- The shared cross-runtime fixture then failed in TypeScript with `calibrating` instead of `stable` before the equivalent implementation was added.
- The repaired rule follows a provisional reset only while every accepted active-cycle reading remains 0% and the reset shift matches the observation gap within five minutes. Positive usage ends that exception immediately.
- The exact squash-merged `main` commit passed 107 Node/Vitest tests, 91 Swift tests, TypeScript and Swift builds, the cross-language core specification, lint, repository policy, the 49-file Weekly Only audit, packaging, archive integrity, and `git diff --check`.

## Installed app verification

- The installed branch candidate and exact merged-main build both showed 5% used and a July 22 10:21 local reset while the live read-only probe returned the same 5% used, 95% remaining, and reset timestamp.
- The read-only SQLite query showed consecutive successful 4% and 5% raw readings for that same reset cluster.
- Across automatic refreshes before and after merge, the expanded panel stayed at the current 5% reading, preserved the 10:21 reset, and advanced its data-update timestamp instead of returning to calibration.
- The installed app reports version `0.3.3`, commit `3a335b490c86`, and the empty-content source-patch fingerprint `e3b0c442...b855`. One process runs from `/Applications/Quota Capsule Beta.app`, and its ad-hoc signature passes strict verification.
- No Codex auth, configuration, binary, or login state was changed.

## Code review

- The exception is restricted to the active cycle, requires every accepted value in that cycle to remain exactly 0%, and requires reset drift to track the elapsed observation time within the pre-existing five-minute tolerance.
- Replacing earlier zero-only provisional observations avoids inventing a pace from a moving cycle start and bounds retained history without altering any confirmed positive-use segment.
- Genuine reset candidates, downward corrections, alternating streams, stale reads, and positive-use reset changes retain their prior quarantine behavior.

## Pull request

- Public issue [#23](https://github.com/Bono12138/codex-quota-capsule/issues/23) is the P0 source of truth.
- Public pull request [#24](https://github.com/Bono12138/codex-quota-capsule/pull/24) passed the required `node` and `macos-swift` checks, squash-merged as `3a335b490c868b4e7a9e2fed2ee399b7c68d1b26`, and automatically closed #23.
- Every PR commit, the squash merge, the tag, and the release resolve to repository owner `Bono12138`.

## Publication verification

- Tag `v0.3.3-beta.1` points to exact release commit `3a335b490c868b4e7a9e2fed2ee399b7c68d1b26`.
- Tag-triggered [CI run 29393110526](https://github.com/Bono12138/codex-quota-capsule/actions/runs/29393110526) passed both `node` and `macos-swift`.
- The [GitHub prerelease](https://github.com/Bono12138/codex-quota-capsule/releases/tag/v0.3.3-beta.1) is public and contains `Quota-Capsule-Beta-macOS.zip`.
- The final local ZIP and a fresh download of the release asset both produced SHA-256 `4dc07ef839f9fc65693f3c323b0f05095c944d892028e3e12460bbf5b27c52a9`; the downloaded archive also passed integrity testing.
- The generated app bundle under `dist/beta` is transient; the verified ZIP and installed `/Applications` app are retained.

## Release status

`v0.3.3-beta.1` is released. Public PR and tag CI, exact merged-main packaging and installation, live source/database/UI agreement across automatic refresh, signature verification, archive integrity, and remote asset checksum verification all passed.
